import { mapServicesToSchema } from '../makeMergedSchema/makeMergedSchema'

describe('makeMergedSchema', () => {
  describe('mapServicesToSchema', () => {
    // given a bunch of schemas and services it correctly determines
    // which functios are missing from the schema and grab them
    // from the services instead
    // if you're developing then it warns you.
  })
})
