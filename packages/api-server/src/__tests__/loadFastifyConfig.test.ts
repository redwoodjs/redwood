test('Supports old format of exporting config directly ', () => {
  // Implement me
  expect('fail').toBe(true)
})

test('Supports new form of exporting fastify config', () => {
  // Implement me
  expect('fail').toBe(true)
})
