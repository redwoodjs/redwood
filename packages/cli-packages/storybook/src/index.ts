import {
  command,
  aliases,
  description,
  builder,
  handler,
} from './commands/storybook'

export const commands = [
  {
    command,
    aliases,
    description,
    builder,
    handler,
  },
]
