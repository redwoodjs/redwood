export const schema = gql`
  type User {
    id: Int!
    name: String
    email: String!
    isAdmin: Boolean!
  }

  type Query {
    users: [User]
  }

  input UserInput {
    name: String
    email: String
    isAdmin: Boolean
  }
`
