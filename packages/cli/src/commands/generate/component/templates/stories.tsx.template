import type { ComponentStory } from '@storybook/react'

import ${pascalName} from './${pascalName}'

export const generated: ComponentStory<typeof ${pascalName}> = (args) => {
  return <${pascalName} {...args} />
}

export default { title: 'Components/${pascalName}', component: ${pascalName} }
