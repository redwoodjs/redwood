import type { Story } from '@storybook/react'

import ${pascalName} from './${pascalName}'

export const generated: Story<typeof ${pascalName}> = (args) => {
  return <${pascalName} {...args} />
}

export default { title: 'Components/${pascalName}' }
