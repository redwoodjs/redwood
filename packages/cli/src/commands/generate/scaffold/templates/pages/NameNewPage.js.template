import ${pluralPascalName}Layout from '${importNamesLayout}'
import ${singularPascalName}New from '${importNameNew}'

const ${singularPascalName}NewPage = () => {
  return (
    <${pluralPascalName}Layout>
      <${singularPascalName}New />
    </${pluralPascalName}Layout>
  )
}

export default ${singularPascalName}NewPage
