import ${pluralPascalName}Layout from 'src/layouts/${camelScaffoldPath}${pluralPascalName}Layout'
import Edit${singularPascalName}Cell from 'src/components/${camelScaffoldPath}Edit${singularPascalName}Cell'

const Edit${singularPascalName}Page = ({ id }) => {
  return (
    <${pluralPascalName}Layout>
      <Edit${singularPascalName}Cell id={id} />
    </${pluralPascalName}Layout>
  )
}

export default Edit${singularPascalName}Page
