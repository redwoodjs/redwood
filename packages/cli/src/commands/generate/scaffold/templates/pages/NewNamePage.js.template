import ${pluralPascalName}Layout from 'src/layouts/${camelScaffoldPath}${pluralPascalName}Layout'
import New${singularPascalName} from 'src/components/${camelScaffoldPath}New${singularPascalName}'

const New${singularPascalName}Page = () => {
  return (
    <${pluralPascalName}Layout>
      <New${singularPascalName} />
    </${pluralPascalName}Layout>
  )
}

export default New${singularPascalName}Page
