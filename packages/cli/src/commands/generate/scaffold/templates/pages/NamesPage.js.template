import ${pluralPascalName}Layout from 'src/layouts/${camelScaffoldPath}${pluralPascalName}Layout'
import ${pluralPascalName}Cell from 'src/components/${camelScaffoldPath}${pluralPascalName}Cell'

const ${pluralPascalName}Page = () => {
  return (
    <${pluralPascalName}Layout>
      <${pluralPascalName}Cell />
    </${pluralPascalName}Layout>
  )
}

export default ${pluralPascalName}Page
