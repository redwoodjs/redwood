import ${pluralPascalName}Layout from '${importNamesLayout}'
import ${pluralPascalName}Cell from '${importNamesCell}'

const ${pluralPascalName}Page = () => {
  return (
    <${pluralPascalName}Layout>
      <${pluralPascalName}Cell />
    </${pluralPascalName}Layout>
  )
}

export default ${pluralPascalName}Page
