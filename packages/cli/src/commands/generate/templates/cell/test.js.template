import React from 'react';
import { fireEvent, cleanup } from '@testing-library/react'

import ${pascalName}Cell from './${pascalName}Cell'

describe('${pascalName}Cell', () => {
  it('is true', () => {
    expect(true).toBe(true)
  })
})
