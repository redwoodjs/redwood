// Define your own mock data here:
export const standard = (/* vars, { ctx, req } */) => ({
  ${camelName}: [
    { id: ${mockIdValues[0]} },
    { id: ${mockIdValues[1]} },
    { id: ${mockIdValues[2]} }
  ]
  })
