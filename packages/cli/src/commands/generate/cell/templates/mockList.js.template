// Define your own mock data here:
export const standard = (/* vars, { ctx, req } */) => ({
  ${camelName}: [
    {
      __typename: "${singularPascalName}",
      ${idName}: ${mockIdValues[0]} },
    {
      __typename: "${singularPascalName}",
      ${idName}: ${mockIdValues[1]} },
    {
      __typename: "${singularPascalName}",
      ${idName}: ${mockIdValues[2]} }
    ]
  })
