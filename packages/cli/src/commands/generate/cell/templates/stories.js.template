import { Loading, Empty, Failure, Success } from './${pascalName}'


export const loading = () => {
  return <Loading />
}

export const empty = () => {
  return <Empty />
}

export const failure = () => {
  return <Failure />
}

export const success = () => {
  return <Success />
}

export default { title: '${pascalName}' }
