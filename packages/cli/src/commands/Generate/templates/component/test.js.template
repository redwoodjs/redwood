import React from 'react';
import { fireEvent, cleanup } from '@testing-library/react'

import ${pascalName} from './'

describe('${pascalName}', () => {

  afterEach(() => {
    cleanup()
  })

  it('this test will fail', () => {
    const component = renderComponent(<${pascalName} />)
    component.debug();
    expect(true).toBe(false)
  })
})
