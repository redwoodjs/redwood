{
  "scripts": {
    "postdeploy": "yarn install --immutable && yarn rw build"
  },
  "env": {
    "SESSION_SECRET": {
      "description": "generated session secret per deploy",
      "generator": "secret"
    }
  },
  "addons": [
    {
      "plan": "heroku-postgresql",
      "options": {
        "version": "14"
      }
    }
  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    },
    {
      "url": "heroku-community/nginx"
    }
  ]
}
