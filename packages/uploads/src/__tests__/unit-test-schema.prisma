datasource db {
  provider = "sqlite"
  url      = "file:for_unit_test.db"
}

generator client {
  provider = "prisma-client-js"
}

model Dummy {
  id Int @id @default(autoincrement())
  uploadField String
}

model Dumbo {
  id Int @id @default(autoincrement())
  firstUpload String
  secondUpload String
}
